# ── Secretos y configuración local ──
backend/.env
.env
.env.local
# Las copias a mano del .env (en la VPS hay una, backend/.env.bak-2026-08-30):
# llevan los mismos secretos y un `git add -A` las subiría.
.env.bak*
*.env.bak*

# ── Base de datos local y estado de tatamis ──
backend/instance/

# ── Python ──
__pycache__/
*.pyc
backend/venv/

# ── Logs de desarrollo ──
backend-dev.log
backend-dev.err
*.log
*.err
*.tsbuildinfo

# ── Node (refuerzo del .gitignore del frontend) ──
node_modules/
