Flask==3.1.1
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-SocketIO==5.5.1
Flask-JWT-Extended==4.7.1
# cryptography: la necesita PyJWT (que ya viene con Flask-JWT-Extended) para
# verificar RS256 — el algoritmo con el que firma el ecosistema. Sin ella, el
# pase de DINAMYT se rechaza con «Algorithm not supported», que no dice nada
# de una dependencia que falta. Los tokens propios son HS256 y no la usan, así
# que hasta ahora no hacía falta. Ver app/identidad.py.
cryptography>=43,<47
Flask-CORS==5.0.1
python-dotenv==1.1.0
# tzdata: la base de datos de husos horarios que `zoneinfo` necesita para
# resolver "America/Bogota" (ver app/timeutil.py). Windows no trae ninguna, y
# una imagen Linux mínima tampoco tiene por qué: sin esto los reportes salen
# con la hora del proceso, que en Render es UTC.
tzdata==2025.2
bcrypt==4.3.0
openpyxl==3.1.5
reportlab==4.4.1
# psycopg2-binary: solo se instala en producción (Python <= 3.13, ej. Render).
# En Windows local con Python 3.14 se omite automáticamente (SQLite local).
psycopg2-binary==2.9.10; python_version < "3.14"
# eventlet: las versiones nuevas (0.36+) tienen roto el monkey-patching de
# locks bajo gunicorn: todo acceso al pool de SQLAlchemy muere con
# "RuntimeError: cannot notify on un-acquired lock" (en Python 3.11 y 3.12).
# Producción (Render, Python 3.11.9) usa la 0.33.3, estable por años con
# Flask-SocketIO. La 0.33.3 no instala en Python >= 3.12 (usa distutils),
# así que el entorno local Windows (3.14) conserva la 0.39.1 — ahí no hay
# gunicorn ni monkey-patching y el bug no aplica.
eventlet==0.33.3; python_version < "3.12"
eventlet==0.39.1; python_version >= "3.12"
gunicorn==23.0.0
# simple-websocket: habilita WebSocket real en el modo "threading" (despliegue
# local). Sin él, Socket.IO cae a long-polling (funciona igual, solo más pesado).
simple-websocket>=1.0.0
